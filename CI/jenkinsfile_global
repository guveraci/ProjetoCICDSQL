pipeline {
    agent any
    
    stages {
        stage('Version') {
            steps {
                pwsh "--version"
            }
        }
        
        stage ("PowerShell") {

            steps {
                pwsh "devops/gera-pacote.ps1" 
            }
        
            }
        }
    }