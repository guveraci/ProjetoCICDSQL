pipeline {
    agent any
    
    stages {
        
        stage ("PowerShell") {

            steps {
                powershell (script: 'gera-pacote.ps1')
            }
        
            }
        }
    }