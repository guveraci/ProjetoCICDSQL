pipeline {
    agent any
    
    stages {
        stage('Version') {
            steps {
                powershell "--version"
            }
        }
        
        stage ("PowerShell") {

            steps {
                pwsh "devops/gera-pacote.ps1" 
            }
        
            }
        }
    }