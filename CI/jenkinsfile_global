pipeline {
    agent any
    
    stages {
        stage('Teste') {
            steps {
                sh "pwsh --version"
            }
        }
        
        stage ("PowerShell") {

            steps {
                sh 'pwsh devops/gera-pacote.ps1' 
            }
        
            }
        }
    }