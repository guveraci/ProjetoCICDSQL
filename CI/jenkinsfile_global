pipeline {
    agent any
    
    stages {
        stage('Version') {
            steps {
                sh "pwsh --version"
            }
        }
        
        stage ("PowerShell") {

            steps {
                sh "pwsh devops/gera-pacote.ps1" 
            }
        
            }
        }
    }